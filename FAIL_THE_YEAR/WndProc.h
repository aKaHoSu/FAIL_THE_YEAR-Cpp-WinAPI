/* =============================================================================
 * ファイル概要:
 * - ウィンドウプロシージャ WndProc の宣言を記述する。
 * ============================================================================= */
#pragma once

// WndProc 関数の宣言
LRESULT CALLBACK WndProc(
	HWND hWnd,
	UINT message,
	WPARAM wParam,
	LPARAM lParam
);

